package com.example.interviewquestion;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
    }

//                binding.questionTxt.setText(multipleChoiceList.get(0).getQuestion());
//                binding.answerARb.setText(multipleChoice.getAnswers().getAnswerA());
//                binding.answerBRb.setText(multipleChoice.getAnswers().getAnswerB());
//                binding.answerCRb.setText(multipleChoice.getAnswers().getAnswerC());
//                binding.answerDRbtn.setText(multipleChoice.getAnswers().getAnswerD());
//                binding.answerERb.setText(multipleChoice.getAnswers().getAnswerE());
//                binding.answerFfRb.setText(multipleChoice.getAnswers().getAnswerF());
}